
{
	"name": "s3",
   "comment": "Module for retrieving S3 objects.",
   "connectedApp": "default",
	"description": "Module for retrieving S3 objects.",
   "files": ["S3.php"],
	"routes": {
      "legal-resource-library":{
         "callback": "doAction",
         "content-type": "text/html",
			"access":"is_authenticated",
			"authorization":"webserver"
      }
    }
}